[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]
# E/F：常见错误与潜在 bug（pycodestyle + pyflakes）
# I：import 排序（isort 类似）

[tool.ruff.format]
quote-style = "double"
